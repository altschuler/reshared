fragment UserCard on users {
    id
    display_name
    avatar_url
}

fragment UserDetail on users {
    ...UserCard
    created_at
}

query UserList {
    users {
        ...UserCard
    }
}

query UserDetails($id: uuid!) {
    users_by_pk(id: $id) {
        ...UserDetail
    }
}
